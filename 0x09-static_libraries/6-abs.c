#include "main.h"

/**
 * _abs - this computes the absolute value of an integer
 * @n: the input
 * Return: The absolute value
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
